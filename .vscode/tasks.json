{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "n build",
            "type": "shell",
            "options": {
                "cwd": "${workspaceRoot}/adenos"
            },
            "command": "n build"
        },
        {
            "label": "n debug",
            "type": "shell",
            "options": {
                "cwd": "${workspaceRoot}/adenos"
            },
            "command": "n debug"
        }
    ]
}